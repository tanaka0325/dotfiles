local p = "config."
require(p.."keymaps")
require(p.."options")
require(p.."commands")
require(p.."lazy")

local l = "languages."
require(l.."protobuf")
require(l.."markdown")
require(l.."env")
require(l.."yo-tmpl")
